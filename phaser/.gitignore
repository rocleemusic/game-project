node_modules/
dist/

# Generated by `npm run prep:content` from content/ and lantern-projects/v01.
# Checked out fresh rather than committed, so the probe can never carry a stale
# copy of the run folder or of a spell record.
public/story/
public/content/
public/run-images/
.adversary

# Per-machine itch.io push target (tools/deploy-itch.mjs) — not content.
itch.config.json

# itch.io's butler CLI, downloaded by tools/deploy-itch.mjs's setup step —
# a per-machine binary, not something to commit.
tools/bin/
